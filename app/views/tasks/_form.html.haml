= simple_form_for @task, html: { class: "form-horizontal" } do |f|
  = f.input :label
  = f.ckeditor :html_form
  = f.submit nil, class: 'btn btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), tasks_path, :class => 'btn btn-default'
